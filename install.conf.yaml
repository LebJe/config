- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/config: ''
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc
    ~/.oh-my-zsh: oh-my-zsh
- shell:
  - [git submodule update --init --recursive, Installing submodules]
