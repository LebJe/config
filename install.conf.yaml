- defaults:
    link:
      relink: true

- clean: ["~", "~/config/"]

- link:
    ~/config: ''
    ~/.zshrc:
    ~/cronFiles:
    ~/.config/nvim/init.vim:
- shell:
  - './installVimPlug.sh'
  - 'echo ":q" | vim -c ":PlugInstall"'
