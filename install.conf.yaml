- defaults:
      link:
          relink: true

- clean: ["~", "~/config/"]

- link:
      ~/config: ""
      ~/.zshrc:
      ~/.config/nvim: nvim
      ~/.prettierrc.toml:
      ~/.wgetrc:
      ~/.tmux.conf:
      ~/.config/starship.toml:
      /usr/local/bin/temp:
