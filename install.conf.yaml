- defaults:
    link:
      relink: true

- clean: ["~", "~/config/"]

- link:
    ~/config: ''
    ~/.zshrc:
    ~/cronFiles:
    ~/.config/nvim/init.vim:
- shell:
  - 'echo ":q" | vim -c ":PlugInstall"'
  - './installVimPlug.sh'
